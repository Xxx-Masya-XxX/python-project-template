"""
Пример пользовательского плагина для сортировщика сериалов
"""

def get_plugin_info():
    """
    Обязательная функция - возвращает информацию о плагине
    """
    return {
        'name': 'Пример Плагина',
        'description': 'Демонстрирует как создавать плагины для сортировщика',
        'version': '1.0.0',
        'author': 'Разработчик',
        'icon': '🎯'
    }

def run_plugin(context):
    """
    Обязательная функция - выполняет логику плагина
    
    context содержит:
    - source_folder: исходная папка
    - target_folder: целевая папка  
    - move_files: перемещать или копировать
    - log_callback: функция для логирования
    - main_window: ссылка на главное окно
    """
    log = context['log_callback']
    
    log("INFO", "Запуск примера плагина...")
    log("INFO", f"Исходная папка: {context['source_folder']}")
    log("INFO", f"Целевая папка: {context['target_folder']}")
    log("INFO", f"Режим: {'Перемещение' if context['move_files'] else 'Копирование'}")
    
    # Здесь ваша логика
    import time
    time.sleep(1)  # Имитация работы
    
    log("SUCCESS", "Пример плагина выполнен успешно!")
    
    return {
        'success': True,
        'message': 'Плагин отработал корректно',
        'processed_files': 0
    }